# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  name: MyString
  user: one # Assumes a user fixture named 'one' exists in users.yml

two:
  name: AnotherString
  user: two # Assumes a user fixture named 'two' exists in users.yml
